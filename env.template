# Koneksi PostgreSQL (warehouse)
POSTGRES_URI=postgresql+psycopg2://etl_user:etl_pass@localhost:5432/etl_warehouse

# (Opsional) Sumber MySQL untuk extract nyata
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=secret
MYSQL_DATABASE=core_banking

# (Opsional) External API mock
API_URL=

# Direktori data
DATA_DIR=./data

